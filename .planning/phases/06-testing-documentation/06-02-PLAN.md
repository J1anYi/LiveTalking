---
phase: 06-testing-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_database_connector.py
autonomous: true
requirements:
  - NFR-3

must_haves:
  truths:
    - "SQLiteConnector 的连接、查询、文档转换功能有单元测试"
    - "BaseDatabaseConnector 的辅助方法有测试"
    - "测试使用内存数据库，不依赖外部数据库"
  artifacts:
    - path: "tests/test_database_connector.py"
      provides: "数据库连接器单元测试"
      exports: ["TestSQLiteConnector", "TestBaseDatabaseConnector"]
    - path: "tests/test_database_connector.py"
      contains: "def test_sqlite_connector_connect"
  key_links:
    - from: "tests/test_database_connector.py"
      to: "rag/loaders/database_connector.py"
      via: "import"
      pattern: "from rag.loaders import SQLiteConnector"
---

<objective>
为 rag/loaders/database_connector.py 创建单元测试，覆盖 SQLiteConnector 和 BaseDatabaseConnector 的核心功能。

Purpose: 确保 Phase 3 实现的数据库连接器功能正确，满足 D-01 核心模块优先测试策略
Output: tests/test_database_connector.py 包含 10+ 个测试用例
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-testing-documentation/06-CONTEXT.md
</context>

<interfaces>
<!-- Executor 应直接使用这些接口 -->

From rag/loaders/database_connector.py:
```python
class BaseDatabaseConnector(ABC):
    def __init__(self, connection_string, query, query_params=None, content_columns=None, metadata_columns=None, db_type="unknown"): ...
    @abstractmethod
    def connect(self) -> None: ...
    @abstractmethod
    def disconnect(self) -> None: ...
    @abstractmethod
    def execute_query(self, query: str, params: dict | None = None) -> list[dict]: ...
    def load(self) -> list[tuple[str, dict]]: ...
    def _row_to_document(self, row: dict, row_idx: int) -> tuple[str, dict]: ...
    def _safe_connection_string(self) -> str: ...
    def _extract_table_name(self) -> str | None: ...

class SQLiteConnector(BaseDatabaseConnector):
    def connect(self) -> None: ...
    def disconnect(self) -> None: ...
    def execute_query(self, query: str, params: dict | None = None) -> list[dict]: ...
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create test_database_connector.py with comprehensive tests</name>
  <files>tests/test_database_connector.py</files>
  <read_first>
    - E:\code_github\LiveTalking3\LiveTalking\rag\loaders\database_connector.py (被测试模块)
    - E:\code_github\LiveTalking3\LiveTalking\tests\test_retriever.py (测试模式参考)
  </read_first>
  <action>
    创建 `tests/test_database_connector.py`，实现以下测试类和测试方法：

    ## TestSQLiteConnector (8 tests)
    使用 `:memory:` 作为 SQLite 内存数据库进行测试。

    1. `test_connect_creates_connection` - 测试 connect() 方法建立连接
    2. `test_disconnect_closes_connection` - 测试 disconnect() 方法关闭连接
    3. `test_execute_query_returns_results` - 创建内存表、插入数据、查询验证结果
       ```python
       # 创建测试表
       connector.connect()
       connector._connection.execute("CREATE TABLE test (id INTEGER, name TEXT)")
       connector._connection.execute("INSERT INTO test VALUES (1, 'alice')")
       connector._connection.commit()
       # 修改查询并执行
       connector._query = "SELECT * FROM test"
       results = connector.execute_query("SELECT * FROM test")
       assert len(results) == 1
       assert results[0]["name"] == "alice"
       ```
    4. `test_execute_query_with_params` - 测试参数化查询
    5. `test_execute_query_raises_when_not_connected` - 未连接时执行查询抛出 RuntimeError
    6. `test_load_returns_documents` - 测试 load() 方法返回文档列表
    7. `test_load_with_content_columns` - 测试指定 content_columns 时只提取指定列
    8. `test_load_with_metadata_columns` - 测试指定 metadata_columns 时添加到元数据

    ## TestBaseDatabaseConnectorHelperMethods (4 tests)
    创建一个简单的测试子类来测试基类的辅助方法。

    9. `test_safe_connection_string_hides_password` - 测试密码隐藏
       ```python
       # 测试 password=xxx 格式
       connector = SQLiteConnector("sqlite:///db.db?password=secret123", "SELECT 1")
       safe = connector._safe_connection_string()
       assert "secret123" not in safe
       assert "***" in safe
       ```
    10. `test_safe_connection_string_hides_pwd` - 测试 pwd=xxx 格式
    11. `test_extract_table_name_from_query` - 测试从 SQL 提取表名
        ```python
        connector._query = "SELECT * FROM users WHERE id = 1"
        assert connector._extract_table_name() == "users"
        ```
    12. `test_extract_table_name_returns_none_for_invalid_query` - 无效查询返回 None

    使用 pytest fixture 创建测试数据库，遵循现有测试文件的模式。
  </action>
  <verify>
    <automated>cd /e/code_github/LiveTalking3/LiveTalking && python -m pytest tests/test_database_connector.py -v --tb=short 2>&1 | head -50</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "def test_connect_creates_connection" tests/test_database_connector.py
    - grep -q "def test_execute_query_returns_results" tests/test_database_connector.py
    - grep -q "def test_safe_connection_string_hides_password" tests/test_database_connector.py
    - grep -c "def test_" tests/test_database_connector.py 应返回 >= 10
    - 测试文件包含 2 个测试类：TestSQLiteConnector, TestBaseDatabaseConnectorHelperMethods
    - 测试使用 `:memory:` 内存数据库
  </acceptance_criteria>
  <done>
    - tests/test_database_connector.py 创建完成
    - 12 个测试用例实现
    - pytest 运行通过
    - 测试覆盖 SQLiteConnector 和 BaseDatabaseConnector 核心功能
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试文件 → 被测模块 | 测试代码可信，无需安全检查 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | - | - | - | 测试代码无安全威胁 |
</threat_model>

<verification>
- 运行 `python -m pytest tests/test_database_connector.py -v` 确认所有测试通过
- 运行 `python -m pytest tests/test_database_connector.py --collect-only` 确认测试数量
</verification>

<success_criteria>
- tests/test_database_connector.py 包含至少 10 个测试用例
- 所有测试通过
- 测试覆盖 SQLiteConnector 的 connect, disconnect, execute_query, load 方法
- 测试覆盖 BaseDatabaseConnector 的 _safe_connection_string, _extract_table_name 辅助方法
- 测试使用内存数据库，不依赖外部文件
</success_criteria>

<output>
After completion, create `.planning/phases/06-testing-documentation/06-02-SUMMARY.md`
</output>
