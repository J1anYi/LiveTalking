---
phase: 06-testing-documentation
plan: 04
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - tests/test_e2e_rag.py
autonomous: true
requirements:
  - NFR-3

must_haves:
  truths:
    - "RAG 全流程可以被端到端测试"
    - "LLM 集成可以被测试"
    - "配置系统可以被端到端测试"
    - "测试使用真实 DashScope Embedding API"
  artifacts:
    - path: "tests/test_e2e_rag.py"
      provides: "端到端集成测试"
      exports: ["TestRAGEndToEnd", "TestLLMIntegration", "TestConfigSystem"]
    - path: "tests/test_e2e_rag.py"
      contains: "def test_rag_full_pipeline"
  key_links:
    - from: "tests/test_e2e_rag.py"
      to: "rag/"
      via: "import"
      pattern: "from rag import"
---

<objective>
创建端到端集成测试，覆盖 RAG 全流程、LLM 集成和配置系统三个场景。

Purpose: 验证系统整体功能正常，满足 D-05 端到端测试覆盖要求
Output: tests/test_e2e_rag.py 包含 3 个测试类的集成测试
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-testing-documentation/06-CONTEXT.md
</context>

<interfaces>
<!-- Executor 应直接使用这些接口 -->

From tests/test_integration.py (existing pattern):
```python
def test_end_to_end_flow_mocked(self):
    """Test end-to-end RAG flow with mocked API."""
    from rag import (
        DashScopeEmbedding,
        VectorStore,
        DocumentProcessor,
        RAGRetriever,
        build_rag_prompt,
    )
    # Uses tempfile.TemporaryDirectory for isolation
```

API Key from D-04:
```
API Key: sk-a3451ca04a30450a97c5da171f6be961
Base URL: https://dashscope.aliyuncs.com/compatible-mode/v1
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create end-to-end integration tests</name>
  <files>tests/test_e2e_rag.py</files>
  <read_first>
    - E:\code_github\LiveTalking3\LiveTalking\tests\test_integration.py (现有集成测试模式)
    - E:\code_github\LiveTalking3\LiveTalking\rag\__init__.py (公共 API)
    - E:\code_github\LiveTalking3\LiveTalking\rag\config_loader.py (配置加载器)
    - E:\code_github\LiveTalking3\LiveTalking\.planning\phases\06-testing-documentation\06-CONTEXT.md (D-04: API Key)
  </read_first>
  <action>
    创建 `tests/test_e2e_rag.py`，实现以下测试类和测试方法：

    ## TestRAGEndToEnd (3 tests)
    测试 RAG 全流程：文档加载 -> 分块 -> Embedding -> 存储 -> 检索

    1. `test_rag_full_pipeline_real_api` - 使用真实 DashScope API 测试完整流程
       ```python
       import os
       import tempfile
       import pytest
       from rag import (
           DashScopeEmbedding, VectorStore, DocumentProcessor,
           RAGRetriever, FileLoader, build_rag_prompt
       )

       @pytest.mark.integration
       def test_rag_full_pipeline_real_api(self):
           """Test RAG full pipeline with real DashScope API."""
           # 从环境变量读取 API Key（D-04）
           # 测试前需设置: export DASHSCOPE_API_KEY=sk-a3451ca04a30450a97c5da171f6be961
           api_key = os.environ.get("DASHSCOPE_API_KEY")
           if not api_key:
               pytest.skip("DASHSCOPE_API_KEY not set, skipping integration test")

           with tempfile.TemporaryDirectory() as tmpdir:
               # 1. 创建测试文档
               doc_path = os.path.join(tmpdir, "test.txt")
               with open(doc_path, "w") as f:
                   f.write("RAG 是 Retrieval Augmented Generation 的缩写。")
                   f.write("它结合了检索和生成技术。")

               # 2. 加载文档
               loader = FileLoader()
               content, metadata = loader.load(doc_path)

               # 3. 分块
               processor = DocumentProcessor(chunk_size=200, chunk_overlap=50)
               chunks = processor.process_text(content, metadata=metadata)

               # 4. 初始化 Embedding 和 VectorStore
               embedding = DashScopeEmbedding()
               store = VectorStore(persist_dir=os.path.join(tmpdir, "chromadb"))
               retriever = RAGRetriever(store, embedding, top_k=3)

               # 5. 索引文档
               count = retriever.ingest(chunks)
               assert count > 0

               # 6. 检索
               results = retriever.retrieve("什么是 RAG？")
               assert results is not None
               assert len(results) > 0

               # 7. 构建 prompt
               prompt = build_rag_prompt("什么是 RAG？", results)
               assert "RAG" in prompt
       ```

    2. `test_rag_persistence_across_sessions` - 测试向量存储持久化
       ```python
       def test_rag_persistence_across_sessions(self):
           """Test that knowledge base persists across sessions."""
           with tempfile.TemporaryDirectory() as tmpdir:
               # Session 1: 创建知识库
               store1 = VectorStore(persist_dir=tmpdir)
               embedding = DashScopeEmbedding()
               retriever1 = RAGRetriever(store1, embedding)
               retriever1.ingest([{"text": "测试文档", "metadata": {}}])

               # Session 2: 验证持久化
               store2 = VectorStore(persist_dir=tmpdir)
               assert store2.count() > 0
       ```

    3. `test_rag_with_filter_metadata` - 测试元数据过滤检索

    ## TestLLMIntegration (2 tests)
    测试 LLM 与 RAG 的集成

    4. `test_llm_rag_context_injection` - 测试 RAG 上下文注入到 LLM prompt
       ```python
       def test_llm_rag_context_injection(self):
           """Test that RAG context is injected into LLM prompt."""
           from rag import build_rag_prompt

           chunks = [
               {"text": "知识库内容：RAG 是检索增强生成。"},
           ]

           prompt = build_rag_prompt("什么是 RAG？", chunks)

           # 验证 prompt 结构
           assert "知识库内容" in prompt
           assert "什么是 RAG？" in prompt
           assert "KB content" in prompt
       ```

    5. `test_llm_disabled_rag_no_retrieval` - 测试禁用 RAG 时不进行检索
       ```python
       def test_llm_disabled_rag_no_retrieval(self):
           """Test that RAG is not called when disabled."""
           from rag import get_default_config

           config = get_default_config()
           assert config["enabled"] == False
       ```

    ## TestConfigSystem (3 tests)
    测试配置系统

    6. `test_config_priority_cli_over_env` - 测试 CLI 参数优先级高于环境变量
       ```python
       def test_config_priority_cli_over_env(self, monkeypatch):
           """Test CLI config overrides environment variables."""
           import os
           from rag import merge_rag_config, load_rag_config_from_env

           # 设置环境变量
           monkeypatch.setenv("RAG_TOP_K", "3")

           # CLI 参数
           cli_config = {"top_k": 10}
           env_config = load_rag_config_from_env()
           file_config = {}

           merged = merge_rag_config(cli_config, env_config, file_config)

           assert merged["top_k"] == 10  # CLI 优先
       ```

    7. `test_config_priority_env_over_file` - 测试环境变量优先级高于配置文件
       ```python
       def test_config_priority_env_over_file(self, monkeypatch, tmp_path):
           """Test env config overrides file config."""
           from rag import merge_rag_config, load_rag_config_from_env, save_rag_config

           # 创建配置文件
           file_config = {"top_k": 3}
           config_path = tmp_path / "config.yaml"
           save_rag_config(file_config, str(config_path))

           # 设置环境变量
           monkeypatch.setenv("RAG_TOP_K", "5")
           env_config = load_rag_config_from_env()

           merged = merge_rag_config({}, env_config, file_config)

           assert merged["top_k"] == 5  # 环境变量优先
       ```

    8. `test_config_yaml_loading` - 测试 YAML 配置加载
       ```python
       def test_config_yaml_loading(self, tmp_path):
           """Test loading config from YAML file."""
           from rag import load_rag_config, save_rag_config

           # 创建配置文件
           config = {"enabled": True, "top_k": 5}
           config_path = tmp_path / "rag_config.yaml"
           save_rag_config(config, str(config_path))

           # 加载配置
           loaded = load_rag_config(str(config_path))

           assert loaded["enabled"] == True
           assert loaded["top_k"] == 5
       ```

    使用 pytest marker `@pytest.mark.integration` 标记需要真实 API 的测试。
    测试使用 DASHSCOPE_API_KEY 环境变量或 D-04 提供的 API Key。
  </action>
  <verify>
    <automated>cd /e/code_github/LiveTalking3/LiveTalking && python -m pytest tests/test_e2e_rag.py -v --tb=short -m "not integration" 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "def test_rag_full_pipeline_real_api" tests/test_e2e_rag.py
    - grep -q "def test_llm_rag_context_injection" tests/test_e2e_rag.py
    - grep -q "def test_config_priority_cli_over_env" tests/test_e2e_rag.py
    - grep -c "def test_" tests/test_e2e_rag.py 应返回 >= 8
    - 测试文件包含 3 个测试类：TestRAGEndToEnd, TestLLMIntegration, TestConfigSystem
    - 测试文件包含 `@pytest.mark.integration` 装饰器
  </acceptance_criteria>
  <done>
    - tests/test_e2e_rag.py 创建完成
    - 8 个测试用例实现
    - 非 integration 标记的测试通过
    - 测试覆盖 RAG 全流程、LLM 集成、配置系统三个场景
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试文件 → 外部 API | 使用真实 API Key 进行测试 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Information Disclosure | API Key | mitigate | API Key 仅用于测试，不在代码仓库中提交 |
</threat_model>

<verification>
- 运行 `python -m pytest tests/test_e2e_rag.py -v -m "not integration"` 确认非 API 测试通过
- 运行 `python -m pytest tests/test_e2e_rag.py --collect-only` 确认测试数量
</verification>

<success_criteria>
- tests/test_e2e_rag.py 包含至少 8 个测试用例
- 测试覆盖 RAG 全流程、LLM 集成、配置系统三个场景
- 非 integration 测试全部通过
- 测试使用真实 DashScope API（标记为 @pytest.mark.integration）
- 测试结构清晰，易于维护
</success_criteria>

<output>
After completion, create `.planning/phases/06-testing-documentation/06-04-SUMMARY.md`
</output>
