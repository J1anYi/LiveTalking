---
phase: 06-testing-documentation
plan: 05
type: execute
wave: 3
depends_on:
  - 06-03
files_modified:
  - data/knowledge_base/faq.txt
  - data/knowledge_base/product.md
autonomous: true
requirements:
  - NFR-3

must_haves:
  truths:
    - "用户可以使用示例知识库数据快速体验 RAG 功能"
    - "文档中包含完整的使用流程演示"
    - "示例数据覆盖常见场景"
  artifacts:
    - path: "data/knowledge_base/faq.txt"
      provides: "FAQ 示例数据"
      contains: "常见问题"
    - path: "data/knowledge_base/product.md"
      provides: "产品说明示例数据"
      contains: "功能介绍"
    - path: "docs/rag.md"
      provides: "使用流程演示"
      contains: "## 使用流程演示"
  key_links:
    - from: "docs/rag.md"
      to: "data/knowledge_base/"
      via: "示例引用"
      pattern: "data/knowledge_base/"
---

<objective>
创建示例知识库数据和使用流程演示，帮助用户快速上手 RAG 功能。

Purpose: 满足 D-08 和 D-09 要求，提供可直接使用的示例数据和演示
Output: 示例知识库文件 + 文档中的使用流程演示
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-testing-documentation/06-CONTEXT.md
</context>

<interfaces>
<!-- Executor 应直接使用这些接口 -->

From docs/rag.md (existing documentation):
```markdown
## 快速开始

### 基本用法
1. 启用 RAG 功能
...
```

Expected directory structure:
```
data/
├── knowledge_base/
│   ├── faq.txt          # FAQ 示例
│   └── product.md       # 产品说明示例
└── rag_config.yaml      # 配置文件
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create sample knowledge base data</name>
  <files>data/knowledge_base/faq.txt, data/knowledge_base/product.md</files>
  <read_first>
    - E:\code_github\LiveTalking3\LiveTalking\docs\rag.md (已创建的文档)
    - E:\code_github\LiveTalking3\LiveTalking\CLAUDE.md (项目概述)
  </read_first>
  <action>
    创建示例知识库数据文件：

    ## 1. 创建 `data/knowledge_base/faq.txt`
    内容包含 LiveTalking 系统常见问题，涵盖基本使用、RAG 功能、配置相关、故障排除等主题，至少 10 个问答。

    ## 2. 创建 `data/knowledge_base/product.md`
    内容包含产品说明，涵盖产品概述、核心功能（数字人模型、语音交互、输出方式、RAG 知识库）、架构设计、使用场景、部署要求、快速开始等章节。
  </action>
  <verify>
    <automated>grep -q "常见问题" data/knowledge_base/faq.txt && grep -q "产品说明" data/knowledge_base/product.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "LiveTalking 是什么" data/knowledge_base/faq.txt
    - grep -q "RAG 知识库功能" data/knowledge_base/faq.txt
    - grep -q "如何启用 RAG 知识库" data/knowledge_base/faq.txt
    - grep -q "产品概述" data/knowledge_base/product.md
    - grep -q "核心功能" data/knowledge_base/product.md
    - grep -q "RAG 知识库" data/knowledge_base/product.md
    - faq.txt 包含至少 10 个问答
    - product.md 包含至少 5 个章节
  </acceptance_criteria>
  <done>
    - data/knowledge_base/faq.txt 创建完成，包含常见问答
    - data/knowledge_base/product.md 创建完成，包含产品说明
    - 示例数据可直接用于 RAG 测试
  </done>
</task>

<task type="auto">
  <name>Task 2: Add usage demo section to documentation</name>
  <files>docs/rag.md</files>
  <read_first>
    - E:\code_github\LiveTalking3\LiveTalking\docs\rag.md (已创建的文档)
    - E:\code_github\LiveTalking3\LiveTalking\data\knowledge_base\faq.txt (刚创建的示例数据)
    - E:\code_github\LiveTalking3\LiveTalking\data\knowledge_base\product.md (刚创建的示例数据)
  </read_first>
  <action>
    在 `docs/rag.md` 文档中添加 "使用流程演示" 章节，放在 "FAQ" 章节之前。

    添加以下内容，包含具体的命令行示例：

    ## 使用流程演示

    ### 1. 环境准备
    设置 DashScope API Key：
    ```bash
    # Linux/macOS
    export DASHSCOPE_API_KEY=sk-your-api-key

    # Windows PowerShell
    $env:DASHSCOPE_API_KEY="sk-your-api-key"
    ```

    ### 2. 准备知识库数据
    示例数据位于 `data/knowledge_base/` 目录：
    - `faq.txt` - 常见问题解答
    - `product.md` - 产品功能说明

    ### 3. 启动服务
    ```bash
    # 启用 RAG 功能运行
    python app.py --rag_enabled --rag_top_k 3 --model wav2lip --avatar_id wav2lip256_avatar1

    # 或使用配置文件
    python app.py --rag_config data/rag_config.yaml --model wav2lip --avatar_id wav2lip256_avatar1
    ```

    ### 4. 测试检索功能
    ```bash
    # 使用 curl 测试
    curl -X POST http://localhost:8010/human \
      -H "Content-Type: application/json" \
      -d '{"text": "什么是 RAG 知识库？"}'

    # 预期响应
    # {"response": "RAG 是 Retrieval Augmented Generation 的缩写..."}
    ```

    ### 5. 查看检索结果
    启动时会显示日志：
    ```
    [INFO] RAG enabled, initializing retriever...
    [INFO] RAG retriever initialized, documents: 10
    [INFO] Retrieval query: 什么是 RAG 知识库？
    [INFO] Retrieved 3 chunks
    ```

    ### 6. 验证知识库内容
    ```python
    # Python 直接测试
    from rag import quick_retrieve

    results = quick_retrieve("什么是 RAG？", top_k=3)
    for r in results:
        print(f"Score: {r.get('distance', 'N/A')}")
        print(f"Content: {r['text'][:100]}...")
    ```

    ### 7. 更新知识库
    ```bash
    # 添加新文档到知识库
    cp new_document.txt data/knowledge_base/

    # 重新索引（需要重启服务）
    python app.py --rag_enabled --rag_reindex
    ```

    ### 8. 使用环境变量配置
    ```bash
    # 完整环境变量配置
    export RAG_ENABLED=true
    export RAG_TOP_K=5
    export RAG_PERSIST_DIR=./data/chromadb
    export RAG_COLLECTION=knowledge_base
    export DASHSCOPE_API_KEY=sk-your-api-key

    python app.py --model wav2lip --avatar_id wav2lip256_avatar1
    ```

    ### 9. 完整示例脚本
    ```python
    # test_rag.py
    import os
    from rag import RAGRetriever, VectorStore, DashScopeEmbedding

    # 初始化
    embedding = DashScopeEmbedding()
    store = VectorStore(persist_dir="./data/chromadb")
    retriever = RAGRetriever(store, embedding, top_k=3)

    # 检索
    results = retriever.retrieve("LiveTalking 是什么？")
    print(f"找到 {len(results)} 个相关结果")
    ```
  </action>
  <verify>
    <automated>grep -q "## 使用流程演示" docs/rag.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "## 使用流程演示" docs/rag.md
    - grep -q "export DASHSCOPE_API_KEY" docs/rag.md
    - grep -q "python app.py --rag_enabled" docs/rag.md
    - grep -q "curl -X POST http://localhost:8010/human" docs/rag.md
    - grep -q "from rag import quick_retrieve" docs/rag.md
    - grep -q "export RAG_ENABLED=true" docs/rag.md
    - 文档包含完整的命令行示例和预期输出
  </acceptance_criteria>
  <done>
    - docs/rag.md 包含完整的使用流程演示
    - 演示覆盖环境准备到验证结果的完整流程
    - 包含可复制的命令行示例
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 示例数据 → 用户 | 示例数据公开，无敏感信息 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | - | - | - | 示例数据无安全威胁 |
</threat_model>

<verification>
- 运行 `grep "## 使用流程演示" docs/rag.md` 确认章节存在
- 检查示例数据文件是否创建
</verification>

<success_criteria>
- data/knowledge_base/faq.txt 创建完成，包含至少 10 个问答
- data/knowledge_base/product.md 创建完成，包含产品说明
- docs/rag.md 包含完整的使用流程演示章节
- 演示包含可执行的命令行示例
- 示例数据可直接用于 RAG 测试
</success_criteria>

<output>
After completion, create `.planning/phases/06-testing-documentation/06-05-SUMMARY.md`
</output>
