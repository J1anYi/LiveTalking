---
phase: 06-testing-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_config_loader.py
autonomous: true
requirements:
  - NFR-3

must_haves:
  truths:
    - "config_loader.py 的所有公共函数有单元测试"
    - "测试覆盖正常路径和边界情况"
    - "测试可以独立运行，不依赖外部 API"
  artifacts:
    - path: "tests/test_config_loader.py"
      provides: "config_loader 单元测试"
      exports: ["TestLoadRagConfig", "TestSaveRagConfig", "TestLoadRagConfigFromEnv", "TestMergeRagConfig"]
    - path: "tests/test_config_loader.py"
      contains: "def test_load_rag_config"
  key_links:
    - from: "tests/test_config_loader.py"
      to: "rag/config_loader.py"
      via: "import"
      pattern: "from rag import load_rag_config"
---

<objective>
为 rag/config_loader.py 创建完整的单元测试，覆盖配置加载、保存、环境变量读取和优先级合并功能。

Purpose: 确保 Phase 5 实现的配置加载器功能正确，满足 D-01 核心模块优先测试策略
Output: tests/test_config_loader.py 包含 12+ 个测试用例
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-testing-documentation/06-CONTEXT.md
</context>

<interfaces>
<!-- Executor 应直接使用这些接口 -->

From rag/config_loader.py:
```python
DEFAULT_RAG_CONFIG: dict[str, Any] = {
    "enabled": False,
    "top_k": 3,
    "persist_dir": "./data/chromadb",
    "collection": "knowledge_base",
}

def get_default_rag_config() -> dict[str, Any]: ...
def load_rag_config(filepath: str | None = None) -> dict[str, Any]: ...
def save_rag_config(config: dict[str, Any], filepath: str) -> None: ...
def load_rag_config_from_env() -> dict[str, Any]: ...
def merge_rag_config(cli_config: dict, env_config: dict, file_config: dict) -> dict[str, Any]: ...
```

From tests/test_retriever.py (existing test pattern):
```python
import pytest
from unittest.mock import Mock, patch

class TestRAGRetriever:
    def _create_mocks(self):
        embedding_client = Mock()
        ...
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create test_config_loader.py with comprehensive tests</name>
  <files>tests/test_config_loader.py</files>
  <read_first>
    - E:\code_github\LiveTalking3\LiveTalking\rag\config_loader.py (被测试模块)
    - E:\code_github\LiveTalking3\LiveTalking\tests\test_retriever.py (测试模式参考)
    - E:\code_github\LiveTalking3\LiveTalking\tests\test_integration.py (集成测试模式)
  </read_first>
  <action>
    创建 `tests/test_config_loader.py`，实现以下测试类和测试方法：

    ## TestGetDefaultRagConfig (2 tests)
    1. `test_get_default_rag_config_returns_copy` - 验证返回的是副本，修改不影响默认值
    2. `test_get_default_rag_config_has_required_keys` - 验证包含 enabled, top_k, persist_dir, collection

    ## TestLoadRagConfig (4 tests)
    3. `test_load_rag_config_returns_defaults_when_file_not_exists` - 文件不存在时返回默认值
    4. `test_load_rag_config_loads_yaml_file` - 使用 tempfile 创建临时 YAML 文件测试加载
    5. `test_load_rag_config_handles_empty_file` - 空文件返回默认值
    6. `test_load_rag_config_handles_invalid_yaml` - 无效 YAML 返回默认值（不抛异常）

    ## TestSaveRagConfig (2 tests)
    7. `test_save_rag_config_creates_file` - 保存配置到临时文件，验证文件创建
    8. `test_save_rag_config_creates_parent_directory` - 父目录不存在时自动创建

    ## TestLoadRagConfigFromEnv (4 tests)
    9. `test_load_rag_config_from_env_reads_enabled` - 使用 monkeypatch 或 os.environ 设置 RAG_ENABLED=true 测试
    10. `test_load_rag_config_from_env_reads_top_k` - 设置 RAG_TOP_K=5 测试
    11. `test_load_rag_config_from_env_reads_persist_dir` - 设置 RAG_PERSIST_DIR=/custom/path 测试
    12. `test_load_rag_config_from_env_returns_empty_when_no_env` - 无环境变量时返回空字典

    ## TestMergeRagConfig (4 tests)
    13. `test_merge_rag_config_priority_cli_over_env` - CLI 配置覆盖环境变量
    14. `test_merge_rag_config_priority_env_over_file` - 环境变量覆盖文件配置
    15. `test_merge_rag_config_uses_defaults_for_missing_keys` - 缺失键使用默认值
    16. `test_merge_rag_config_handles_none_values` - None 值不覆盖

    使用 pytest + unittest.mock，遵循现有测试文件的模式。
  </action>
  <verify>
    <automated>cd /e/code_github/LiveTalking3/LiveTalking && python -m pytest tests/test_config_loader.py -v --tb=short 2>&1 | head -50</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "def test_load_rag_config_returns_defaults_when_file_not_exists" tests/test_config_loader.py
    - grep -q "def test_load_rag_config_from_env_reads_enabled" tests/test_config_loader.py
    - grep -q "def test_merge_rag_config_priority_cli_over_env" tests/test_config_loader.py
    - grep -c "def test_" tests/test_config_loader.py 应返回 >= 12
    - 测试文件包含 5 个测试类：TestGetDefaultRagConfig, TestLoadRagConfig, TestSaveRagConfig, TestLoadRagConfigFromEnv, TestMergeRagConfig
  </acceptance_criteria>
  <done>
    - tests/test_config_loader.py 创建完成
    - 16 个测试用例实现
    - pytest 运行通过
    - 测试覆盖 config_loader.py 所有公共函数
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试文件 → 被测模块 | 测试代码可信，无需安全检查 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | - | - | - | 测试代码无安全威胁 |
</threat_model>

<verification>
- 运行 `python -m pytest tests/test_config_loader.py -v` 确认所有测试通过
- 运行 `python -m pytest tests/test_config_loader.py --collect-only` 确认测试数量
</verification>

<success_criteria>
- tests/test_config_loader.py 包含至少 12 个测试用例
- 所有测试通过
- 测试覆盖 config_loader.py 所有公共函数
- 测试使用 tempfile 处理文件操作，不产生残留文件
</success_criteria>

<output>
After completion, create `.planning/phases/06-testing-documentation/06-01-SUMMARY.md`
</output>
