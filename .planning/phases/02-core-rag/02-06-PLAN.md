---
wave: 4
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
  - "02-04"
  - "02-05"
files_modified:
  - rag/__init__.py
  - tests/test_integration.py
  - requirements.txt
autonomous: true
gap_closure: false
requirements_addressed: []
---

# Phase 2 Plan 06: 模块集成与导出

**目标:** 完善 rag 模块的公共 API 导出，添加集成测试，更新依赖。

## 背景

需要确保:
1. 所有组件正确导出
2. 模块可以正常导入
3. 依赖已添加到 requirements.txt
4. 集成测试验证端到端流程

## 任务

### Task 1: 更新 rag/__init__.py

<read_first>
- rag/__init__.py - 当前内容
- rag/embeddings.py - DashScopeEmbedding
- rag/vector_store.py - VectorStore
- rag/document_processor.py - DocumentProcessor
- rag/retriever.py - RAGRetriever
- rag/loaders/file_loader.py - FileLoader
</read_first>

<action>
更新 `rag/__init__.py` 文件:

1. 添加所有实现的导入:
   ```python
   from .embeddings import DashScopeEmbedding
   from .vector_store import VectorStore
   from .document_processor import DocumentProcessor
   from .retriever import RAGRetriever
   from .loaders import FileLoader
   ```

2. 更新 __all__ 列表:
   ```python
   __all__ = [
       "DashScopeEmbedding", "VectorStore", "DocumentProcessor", "RAGRetriever",
       "FileLoader",
       "EmbeddingClient", "VectorStoreProtocol", "DocumentLoader",
       "build_rag_prompt", "quick_retrieve", "get_default_config",
   ]
   ```

3. 更新 quick_retrieve 函数实现:
   - 创建默认配置的检索器
   - 执行检索并返回结果
</action>

<acceptance_criteria>
- `rag/__init__.py` 包含 `from .embeddings import DashScopeEmbedding`
- `rag/__init__.py` 包含 `from .vector_store import VectorStore`
- `rag/__init__.py` 包含 `from .document_processor import DocumentProcessor`
- `rag/__init__.py` 包含 `from .retriever import RAGRetriever`
- `rag/__init__.py` 包含 `from .loaders import FileLoader`
- `rag/__init__.py` 包含 `__all__` 列表
</acceptance_criteria>

---

### Task 2: 创建集成测试

<read_first>
- rag/__init__.py - 模块入口
- rag/retriever.py - RAGRetriever
</read_first>

<action>
创建 `tests/test_integration.py` 文件:

1. 实现 `test_import_all` 测试:
   - 验证所有公共 API 可以导入
   ```python
   from rag import (
       DashScopeEmbedding, VectorStore, DocumentProcessor, RAGRetriever,
       FileLoader, build_rag_prompt, get_default_config
   )
   ```

2. 实现 `test_end_to_end_flow` 测试:
   - 创建临时向量存储目录
   - Mock DashScope API
   - 完整流程: 创建组件 -> 摄入文档 -> 检索

3. 实现 `test_build_rag_prompt` 测试:
   - 验证 prompt 构建正确
   - 验证上下文截断

4. 实现 `test_get_default_config` 测试:
   - 验证返回正确的默认配置
</action>

<acceptance_criteria>
- `tests/test_integration.py` 文件存在
- 文件包含 `def test_import_all`
- 文件包含 `def test_end_to_end_flow`
- 文件包含 `def test_build_rag_prompt`
</acceptance_criteria>

---

### Task 3: 更新 requirements.txt

<read_first>
- requirements.txt - 当前依赖
- .planning/phases/01-research-design/01-VERIFICATION.md - 验证结果
</read_first>

<action>
更新 `requirements.txt` 文件，添加 RAG 相关依赖:

1. 添加核心依赖:
   ```
   # RAG Knowledge Base
   chromadb>=0.5.0
   langchain-text-splitters>=0.3.0
   langchain-core>=0.3.0
   pypdf>=4.0.0
   ```

2. 确保与现有依赖不冲突
</action>

<acceptance_criteria>
- requirements.txt 包含 `chromadb`
- requirements.txt 包含 `langchain-text-splitters`
- requirements.txt 包含 `langchain-core`
- requirements.txt 包含 `pypdf`
</acceptance_criteria>

---

## 验证标准

- [ ] 所有组件正确导出
- [ ] 模块可以 `import rag` 导入
- [ ] 集成测试通过
- [ ] 依赖已添加

## must_haves

- rag 模块可正常导入
- 所有公共 API 在 __all__ 中
- 依赖完整且版本兼容
- 集成测试验证端到端流程
