---
phase: 03-data-source-connectors
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - rag/loaders/api_loader.py
  - rag/loaders/__init__.py
autonomous: true
requirements:
  - FR-1.3

must_haves:
  truths:
    - 用户可以从 REST API 获取知识数据
    - 支持 GET 和 POST 请求方法
    - 支持 Bearer Token 和 API Key 认证
    - 支持 JSONPath 提取响应数据
    - API 数据转换为文档格式
  artifacts:
    - path: rag/loaders/api_loader.py
      provides: REST API 数据加载器
      exports: [APILoader]
    - path: rag/loaders/__init__.py
      provides: 模块导出
---

<objective>
实现 REST API 数据源连接器。
Output: APILoader 类，支持 GET/POST 请求和认证
</objective>

<tasks>

<task type="auto">
  <name>Task 1: 创建 APILoader 类</name>
  <files>rag/loaders/api_loader.py</files>

  <read_first>
    - rag/__init__.py
    - rag/loaders/file_loader.py
  </read_first>

  <action>
    创建 rag/loaders/api_loader.py，实现 APILoader 类：
    
    1. 初始化参数：url, method="GET", headers, body, data_path, auth, timeout=30
    2. 认证支持：Bearer Token 和 API Key (per D-08)
    3. load() 方法：发送 HTTP 请求，解析 JSON，用 JSONPath 提取数据
    4. 元数据格式：source, type="api", method, retrieved_at
    
    per D-06: 支持 GET/POST
    per D-07: 支持 url, method, headers, body, data_path 配置
  </action>

  <acceptance_criteria>
    - grep -n "class APILoader" rag/loaders/api_loader.py 返回行号
    - grep -n "def load" rag/loaders/api_loader.py 返回行号
  </acceptance_criteria>

  <done>
    - APILoader 类实现完成
    - 支持 GET/POST、认证、JSONPath
  </done>
</task>

<task type="auto">
  <name>Task 2: 更新模块导出</name>
  <files>rag/loaders/__init__.py</files>

  <read_first>
    - rag/loaders/__init__.py
  </read_first>

  <action>
    更新 rag/loaders/__init__.py，使用条件导入导出 APILoader
  </action>

  <acceptance_criteria>
    - grep -n "APILoader" rag/loaders/__init__.py 返回行号
  </acceptance_criteria>

  <done>
    - APILoader 正确导出
  </done>
</task>

</tasks>

<success_criteria>
- APILoader 类实现完成
- 导出正确
</success_criteria>
