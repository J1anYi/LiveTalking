---
phase: 03-data-source-connectors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rag/loaders/database_connector.py
  - rag/loaders/__init__.py
  - requirements.txt
autonomous: true
requirements:
  - FR-1.2

must_haves:
  truths:
    - "用户可以从 SQLite 数据库加载知识"
    - "数据库查询结果转换为文档格式"
    - "每一行记录作为一个文档块"
    - "支持自定义 SQL 查询"
  artifacts:
    - path: "rag/loaders/database_connector.py"
      provides: "数据库连接器基类和 SQLite 实现"
      exports: ["BaseDatabaseConnector", "SQLiteConnector"]
      min_lines: 80
    - path: "rag/loaders/__init__.py"
      provides: "模块导出"
      contains: "SQLiteConnector"
  key_links:
    - from: "rag/loaders/database_connector.py"
      to: "DocumentLoader Protocol"
      via: "implements load() method"
      pattern: "def load\(self\) -> list\[tuple\[str, dict\]\]"
---

<objective>
实现数据库连接器基础架构和 SQLite 实现。

Purpose: 为 RAG 系统提供数据库数据源支持，使用统一接口连接和查询数据库。
Output: BaseDatabaseConnector 基类 + SQLiteConnector 实现
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-rag/02-SUMMARY.md
@rag/__init__.py
@rag/loaders/file_loader.py
</context>

<interfaces>
<!-- Executor 必须实现这些接口 -->

From rag/__init__.py (lines 38-40):
```python
@runtime_checkable
class DocumentLoader(Protocol):
    def load(self) -> list[tuple[str, dict]]: ...
```

From rag/loaders/file_loader.py (reference implementation):
```python
class FileLoader:
    def load(self, file_path: str) -> tuple[str, dict]:
        # Returns (content, metadata)
        # metadata contains: source, type, size
```

RAGRetriever.ingest() expects (rag/retriever.py:92-138):
```python
def ingest(self, documents: list[dict], batch_size: int = 20) -> int:
    # documents: List of {"text": str, "metadata": dict}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建 BaseDatabaseConnector 基类</name>
  <files>rag/loaders/database_connector.py</files>

  <read_first>
    - E:\code_github\LiveTalking3\LiveTalking\rag\__init__.py (DocumentLoader Protocol)
    - E:\code_github\LiveTalking3\LiveTalking\rag\loaders\file_loader.py (参考实现模式)
  </read_first>

  <action>
    创建 `rag/loaders/database_connector.py` 文件，实现：

    1. `BaseDatabaseConnector` 抽象基类：
       - 继承 ABC（抽象基类）
       - 实现 DocumentLoader Protocol
       - 抽象方法：
         - `connect()` - 建立数据库连接
         - `disconnect()` - 关闭连接
         - `execute_query(query: str, params: dict | None)` - 执行查询
       - 具体方法：
         - `load() -> list[tuple[str, dict]]` - 实现 Protocol 接口，内部调用 `_execute_and_format()`
         - `_execute_and_format()` - 执行查询并格式化结果
         - `_row_to_document(row: dict) -> tuple[str, dict]` - 将行数据转换为文档格式

    2. 配置参数设计（通过 __init__ 接收）：
       - `connection_string: str` - 数据库连接字符串
       - `query: str` - SQL 查询语句
       - `query_params: dict | None = None` - 查询参数
       - `content_columns: list[str]` - 用于生成文档内容的列名
       - `metadata_columns: list[str] | None = None` - 作为元数据的列名

    3. 元数据格式：
       ```python
       {
           "source": connection_string (隐藏密码),
           "type": "sqlite" | "mysql" | "postgresql",
           "table": 从 query 中提取表名（如果有），
           "row_id": 行标识（如果有的话）
       }
       ```

    per D-01: 使用统一基类 + 数据库特定实现
    per D-02: 遵循 DocumentLoader Protocol 模式，load() 返回 list[tuple[str, dict]]
    per D-03: 使用 SQL 查询配置（非 ORM），保持轻量级
  </action>

  <acceptance_criteria>
    - grep -n "class BaseDatabaseConnector" rag/loaders/database_connector.py 返回行号
    - grep -n "@abstractmethod" rag/loaders/database_connector.py 返回至少 3 处
    - grep -n "def load.*list\[tuple\[str, dict\]\]" rag/loaders/database_connector.py 返回行号
    - 文件包含 from abc import ABC, abstractmethod 导入
  </acceptance_criteria>

  <verify>
    <automated>python -c "from rag.loaders.database_connector import BaseDatabaseConnector; print('OK')"</automated>
  </verify>

  <done>
    - BaseDatabaseConnector 抽象基类创建完成
    - 包含 connect(), disconnect(), execute_query() 抽象方法
    - 实现 load() 方法符合 DocumentLoader Protocol
    - 文件导入无错误
  </done>
</task>

<task type="auto">
  <name>Task 2: 实现 SQLiteConnector</name>
  <files>rag/loaders/database_connector.py, rag/loaders/__init__.py</files>

  <read_first>
    - E:\code_github\LiveTalking3\LiveTalking\rag\loaders\database_connector.py (刚创建的基类)
    - E:\code_github\LiveTalking3\LiveTalking\rag\loaders\__init__.py (当前导出)
  </read_first>

  <action>
    在 `rag/loaders/database_connector.py` 中添加 `SQLiteConnector` 类：

    1. 继承 `BaseDatabaseConnector`

    2. 实现抽象方法：
       ```python
       def connect(self) -> None:
           # 使用 sqlite3.connect(self._connection_string)
           # 设置 row_factory = sqlite3.Row 以支持字典访问

       def disconnect(self) -> None:
           # 关闭连接

       def execute_query(self, query: str, params: dict | None = None) -> list[dict]:
           # 执行查询
           # 返回 dict 列表（每行一个 dict）
       ```

    3. 内容格式化逻辑：
       - `content_columns` 指定的列用换行符连接
       - 格式示例：`"col1: value1\ncol2: value2"`

    4. 使用 Python 标准库 `sqlite3`，无需额外依赖 per D-04

    5. 更新 `rag/loaders/__init__.py`：
       ```python
       from .database_connector import BaseDatabaseConnector, SQLiteConnector

       __all__ = ["FileLoader", "BaseDatabaseConnector", "SQLiteConnector"]
       ```

    per D-04: 首先实现 SQLite（无额外依赖）
  </action>

  <acceptance_criteria>
    - grep -n "class SQLiteConnector" rag/loaders/database_connector.py 返回行号
    - grep -n "class SQLiteConnector.*BaseDatabaseConnector" rag/loaders/database_connector.py 返回行号
    - grep -n "import sqlite3" rag/loaders/database_connector.py 返回行号
    - grep -n "SQLiteConnector" rag/loaders/__init__.py 返回行号
    - __all__ 列表包含 "SQLiteConnector"
  </acceptance_criteria>

  <verify>
    <automated>python -c "from rag.loaders import SQLiteConnector; print('OK')"</automated>
  </verify>

  <done>
    - SQLiteConnector 类实现完成
    - 继承 BaseDatabaseConnector
    - 实现 connect(), disconnect(), execute_query() 方法
    - 模块正确导出 SQLiteConnector
    - 导入测试通过
  </done>
</task>

<task type="auto">
  <name>Task 3: 更新 requirements.txt（如需要）</name>
  <files>requirements.txt</files>

  <read_first>
    - E:\code_github\LiveTalking3\LiveTalking\requirements.txt
  </read_first>

  <action>
    检查 requirements.txt 是否需要更新。

    SQLite 使用 Python 标准库，无需额外依赖。

    per D-04: SQLite 使用标准库 sqlite3，无额外依赖
  </action>

  <acceptance_criteria>
    - 无需添加新依赖（sqlite3 是标准库）
  </acceptance_criteria>

  <verify>
    <automated>python -c "import sqlite3; print(f'sqlite3 OK, version: {sqlite3.sqlite_version}')"</automated>
  </verify>

  <done>
    - 确认 sqlite3 可用（Python 标准库）
    - requirements.txt 无需更新
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Database -> Application | 数据库连接字符串可能包含敏感信息 |
| SQL Query Execution | 用户配置的 SQL 查询可能存在注入风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | connection_string | mitigate | 日志中隐藏密码，使用 `***` 替换 |
| T-03-02 | Injection | execute_query | accept | 使用参数化查询（params dict），由用户负责 SQL 安全 |
| T-03-03 | Tampering | query config | accept | 查询来自配置文件，非用户输入，信任边界内 |
</threat_model>

<verification>
1. 导入测试：所有模块可正常导入
2. 类型检查：SQLiteConnector 符合 DocumentLoader Protocol
3. 基本功能：可以创建 SQLiteConnector 实例
</verification>

<success_criteria>
- BaseDatabaseConnector 抽象基类创建完成
- SQLiteConnector 实现，继承基类
- 导出正确：from rag.loaders import SQLiteConnector 可用
- 符合 DocumentLoader Protocol
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-source-connectors/03-01-SUMMARY.md`
</output>
