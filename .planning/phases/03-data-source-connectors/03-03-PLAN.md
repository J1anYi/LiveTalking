---
phase: 03-data-source-connectors
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01-PLAN.md
  - 03-02-PLAN.md
files_modified:
  - rag/loaders/file_loader.py
  - requirements.txt
autonomous: true
requirements:
  - FR-1.1

must_haves:
  truths:
    - 用户可以加载 DOCX 格式文档
    - DOCX 文档按段落分块
    - 保留标题层级作为 metadata
    - 符合现有 FileLoader 接口
  artifacts:
    - path: rag/loaders/file_loader.py
      provides: 扩展的文件加载器，支持 DOCX
    - path: requirements.txt
      provides: 添加 python-docx 依赖
---

<objective>
扩展 FileLoader 支持 DOCX 格式。

Purpose: 为 RAG 系统添加 Word 文档支持，满足 FR-1.1 中对 Word 文档的需求。
Output: 扩展的 FileLoader 类，支持 .docx 格式
</objective>

<tasks>

<task type="auto">
  <name>Task 1: 添加 python-docx 依赖</name>
  <files>requirements.txt</files>

  <read_first>
    - requirements.txt
  </read_first>

  <action>
    在 requirements.txt 中添加：
    python-docx>=1.1.0
    
    per D-09: 使用 python-docx 库
  </action>

  <acceptance_criteria>
    - grep -n "python-docx" requirements.txt 返回行号
  </acceptance_criteria>

  <verify>
    <automated>pip show python-docx || echo "需要安装: pip install python-docx"</automated>
  </verify>

  <done>
    - python-docx 依赖已添加
  </done>
</task>

<task type="auto">
  <name>Task 2: 扩展 FileLoader 支持 DOCX</name>
  <files>rag/loaders/file_loader.py</files>

  <read_first>
    - rag/loaders/file_loader.py
  </read_first>

  <action>
    扩展 FileLoader 类：
    
    1. 在 DEFAULT_EXTENSIONS 中添加 ".docx"
    
    2. 在 load() 方法中添加 .docx 处理分支：
       ```python
       if ext == ".docx":
           content = self._load_docx(file_path)
       ```
    
    3. 实现 _load_docx() 方法：
       ```python
       def _load_docx(self, file_path: str) -> str:
           from docx import Document
           doc = Document(file_path)
           paragraphs = []
           for para in doc.paragraphs:
               if para.text.strip():
                   paragraphs.append(para.text)
           return "

".join(paragraphs)
       ```
    
    4. 更新 metadata，添加 format: "docx"
    
    per D-09: 扩展 FileLoader 支持 DOCX 格式
    per D-10: DOCX 文档按段落分块，保留标题层级作为 metadata
  </action>

  <acceptance_criteria>
    - grep -n ".docx" rag/loaders/file_loader.py 返回行号
    - grep -n "_load_docx" rag/loaders/file_loader.py 返回行号
    - grep -n "from docx import" rag/loaders/file_loader.py 返回行号
  </acceptance_criteria>

  <verify>
    <automated>python -c "from rag.loaders import FileLoader; f = FileLoader(); print(f.supported_extensions)"</automated>
  </verify>

  <done>
    - FileLoader 支持 .docx 格式
    - 按段落分块
    - 元数据正确
  </done>
</task>

</tasks>

<success_criteria>
- FileLoader 支持 .docx 格式
- 按段落分块处理
- 符合现有接口
</success_criteria>
