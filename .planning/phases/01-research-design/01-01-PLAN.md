---
phase: 01-research-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-research-design/01-VERIFICATION.md
autonomous: true
requirements: [TD-1, TD-2, TD-3]
user_setup: []

must_haves:
  truths:
    - "Developer knows exact DashScope embedding model name"
    - "Developer has confirmed ChromaDB meets <500ms latency for <1M vectors"
    - "Developer has verified Chinese text chunking produces valid segments"
  artifacts:
    - path: ".planning/phases/01-research-design/01-VERIFICATION.md"
      provides: "Verification results for technical decisions"
      contains: "## DashScope Embedding Model"
  key_links:
    - from: "DashScope API"
      to: "llm.py"
      via: "OpenAI-compatible client pattern"
      pattern: "OpenAI.*dashscope"

---

<objective>
Verify technical decisions from research before proceeding to design.

Purpose: Validate assumptions in RESEARCH.md (model names, latency, chunking quality) to avoid costly rework during implementation.
Output: Verification document with confirmed values and any discovered deviations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-research-design/01-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@llm.py

<interfaces>
From llm.py:
```python
from openai import OpenAI

client = OpenAI(
    api_key=os.getenv("DASHSCOPE_API_KEY"),
    base_url="https://dashscope.aliyuncs.com/compatible-mode/v1",
)

# Chat model: "qwen-plus"
completion = client.chat.completions.create(
    model="qwen-plus",
    messages=[...],
    stream=True,
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify DashScope Embedding Model Name</name>
  <files>.planning/phases/01-research-design/01-VERIFICATION.md</files>
  <read_first>
    - E:\code_github\LiveTalking3\LiveTalking\llm.py
    - E:\code_github\LiveTalking3\LiveTalking\.planning\phases\01-research-design\01-RESEARCH.md
  </read_first>
  <action>Create a verification document that records the exact DashScope embedding model name.

1. Check DashScope documentation by searching for embedding model names:
   - Common model names: text-embedding-v1, text-embedding-v2, text-embedding-v3
   - The RESEARCH.md assumed "text-embedding-v2" - verify this

2. Create verification script at `.planning/phases/01-research-design/verify_embedding.py`:
   - Use same OpenAI-compatible client pattern as llm.py
   - Test with: `client.embeddings.create(model="text-embedding-v3", input=["test"])`
   - If fails, try alternative model names

3. Run the script (requires DASHSCOPE_API_KEY env var):
   ```bash
   python .planning/phases/01-research-design/verify_embedding.py
   ```

4. Document results in 01-VERIFICATION.md with:
   - Confirmed model name (e.g., "text-embedding-v3")
   - Embedding dimensions (e.g., 1536)
   - Any API differences from assumed patterns

If DASHSCOPE_API_KEY is not available in environment, document the verification method and mark as "requires API key - manual verification needed".</action>
  <verify>
    <automated>grep -q "## DashScope Embedding Model" .planning/phases/01-research-design/01-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/01-research-design/01-VERIFICATION.md` exists
    - Contains section `## DashScope Embedding Model`
    - Contains line matching pattern `Model: text-embedding-v[0-9]+`
    - Contains line matching pattern `Dimensions: [0-9]+`
  </acceptance_criteria>
  <done>Verification document exists with confirmed DashScope embedding model name and dimensions.</done>
</task>

<task type="auto">
  <name>Task 2: Verify ChromaDB Basic Operations</name>
  <files>.planning/phases/01-research-design/01-VERIFICATION.md</files>
  <read_first>
    - E:\code_github\LiveTalking3\LiveTalking\.planning\phases\01-research-design\01-RESEARCH.md
  </read_first>
  <action>Extend verification document with ChromaDB validation.

1. Create verification script at `.planning/phases/01-research-design/verify_chromadb.py`:
   - Install chromadb if needed: `pip install chromadb`
   - Test basic operations: create collection, add vectors, query
   - Use temporary directory for persistence (cleanup after test)
   - Measure query latency with time.perf_counter()

2. Test script should:
   - Create a collection with 100-1000 test vectors (random embeddings)
   - Query the collection and measure latency
   - Report P50/P95 latency estimates

3. Run the script:
   ```bash
   python .planning/phases/01-research-design/verify_chromadb.py
   ```

4. Add section to 01-VERIFICATION.md:
   - ChromaDB installation status
   - Basic CRUD operations confirmed working
   - Query latency for N vectors (target: less than 500ms for less than 1M vectors)</action>
  <verify>
    <automated>grep -q "## ChromaDB" .planning/phases/01-research-design/01-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 01-VERIFICATION.md contains section `## ChromaDB`
    - Contains line matching pattern `Installation: (success|failed)`
    - Contains line matching pattern `P95 latency: [0-9]+ ms`
  </acceptance_criteria>
  <done>Verification document includes ChromaDB installation status and latency measurements.</done>
</task>

<task type="auto">
  <name>Task 3: Verify Chinese Text Chunking</name>
  <files>.planning/phases/01-research-design/01-VERIFICATION.md</files>
  <read_first>
    - E:\code_github\LiveTalking3\LiveTalking\.planning\phases\01-research-design\01-RESEARCH.md
  </read_first>
  <action>Verify RecursiveCharacterTextSplitter works correctly with Chinese text.

1. Install langchain-text-splitters:
   ```bash
   pip install langchain-text-splitters
   ```

2. Create verification script at `.planning/phases/01-research-design/verify_chunking.py`:
   - Import RecursiveCharacterTextSplitter
   - Use Chinese separators: `["\n\n", "\n", "。", "！", "？", "；", "，", " "]`
   - Test with sample Chinese text containing:
     - Multiple paragraphs
     - Various punctuation (。！？；，)
     - Long sentences

3. Verify chunks:
   - No sentence is cut mid-way (chunks end at punctuation)
   - Chunk sizes are within configured range
   - Overlap is correct

4. Add section to 01-VERIFICATION.md:
   - Installation status
   - Sample input/output showing proper segmentation
   - Configuration recommendations (chunk_size, chunk_overlap)</action>
  <verify>
    <automated>grep -q "## Document Chunking" .planning/phases/01-research-design/01-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 01-VERIFICATION.md contains section `## Document Chunking`
    - Contains line matching pattern `chunk_size: [0-9]+`
    - Contains at least 2 subsections matching pattern `### Chunk [0-9]+`
  </acceptance_criteria>
  <done>Verification document confirms Chinese text chunking works correctly with sample output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local scripts | Verification scripts run locally, no external network exposure |
| DashScope API | External API call requires DASHSCOPE_API_KEY |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | E | API key handling | mitigate | Use environment variables only, never log or print key |
| T-01-02 | I | Verification results | accept | Results contain no sensitive data, only model names and metrics |
</threat_model>

<verification>
- [ ] 01-VERIFICATION.md exists with all three sections
- [ ] DashScope embedding model name confirmed
- [ ] ChromaDB basic operations verified
- [ ] Chinese chunking produces valid segments
</verification>

<success_criteria>
- Technical decisions TD-1 (ChromaDB) and TD-2 (Embedding Model) are validated
- Any deviations from RESEARCH.md assumptions are documented
- Verified values are available for architecture design phase
</success_criteria>

<output>
After completion, create `.planning/phases/01-research-design/01-01-SUMMARY.md`
</output>
