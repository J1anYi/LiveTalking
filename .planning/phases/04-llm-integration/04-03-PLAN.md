---
phase: 04-llm-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01-PLAN.md
  - 04-02-PLAN.md
files_modified:
  - llm.py
autonomous: true
requirements:
  - FR-4.1
  - FR-4.2
  - FR-4.3
  - FR-4.4

must_haves:
  truths:
    - "在 chat 模式下自动检索知识库并注入上下文"
    - "流式输出机制保持不变"
    - "多轮对话维护历史记录用于检索"
    - "echo 模式不执行 RAG 检索"
    - "检索失败时静默降级，继续无知识库对话"
  artifacts:
    - path: "llm.py"
      provides: "RAG 增强的 llm_response 函数"
      contains: "rag_retriever.retrieve"
  key_links:
    - from: "app.rag_retriever"
      to: "llm.llm_response"
      via: "模块导入"
      pattern: "from app import rag_retriever"
---

<objective>
修改 llm.py 集成 RAG 检索逻辑，在 chat 模式下自动检索知识库并注入上下文。

Purpose: 实现 D-01, D-02, D-03, D-05 决策 — 修改 llm_response 函数，通过 RAG 检索增强 prompt，支持多轮对话上下文，静默降级处理错误。
Output: 修改后的 llm.py，包含 RAG 检索逻辑、对话历史管理、知识库 prompt 构建。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-llm-integration/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 RAG 检索导入和模块级变量</name>
  <files>llm.py</files>
  <read_first>
    - E:\code_github\LiveTalking3\LiveTalking\llm.py (当前 LLM 实现)
    - E:\code_github\LiveTalking3\LiveTalking\rag\__init__.py (build_rag_prompt 函数签名)
    - E:\code_github\LiveTalking3\LiveTalking\.planning\phases\04-llm-integration\04-CONTEXT.md (D-02, D-03, D-05 决策)
  </read_first>
  <action>
    在 llm.py 中添加必要的导入和模块级变量：

    1. 在现有导入之后（约第 7 行 `from utils.logger import logger` 之后）添加：
       ```python
       # RAG integration (lazy import to avoid circular dependency)
       rag_retriever = None  # Set by app.py during initialization
       ```

    2. 在 `from utils.logger import logger` 之后添加：
       ```python
       from rag import build_rag_prompt
       ```

    说明：
    - rag_retriever 变量将由 app.py 在初始化后设置，避免循环导入
    - build_rag_prompt 用于构建带知识库上下文的 prompt
  </action>
  <verify>
    <automated>python -c "import llm; print(hasattr(llm, 'rag_retriever'))"</automated>
  </verify>
  <acceptance_criteria>
    - llm.py 包含 `from rag import build_rag_prompt` 导入
    - llm.py 包含模块级变量 `rag_retriever = None`
  </acceptance_criteria>
  <done>llm.py 包含 RAG 相关导入和全局变量声明。</done>
</task>

<task type="auto">
  <name>Task 2: 修改 llm_response 函数添加 RAG 检索逻辑</name>
  <files>llm.py</files>
  <read_first>
    - E:\code_github\LiveTalking3\LiveTalking\llm.py (当前 llm_response 函数实现)
    - E:\code_github\LiveTalking3\LiveTalking\rag\__init__.py (build_rag_prompt 函数签名: build_rag_prompt(query: str, retrieved_chunks: list[dict], max_context_chars: int = 2000) -> str)
    - E:\code_github\LiveTalking3\LiveTalking\rag\retriever.py (RAGRetriever.retrieve 方法签名)
    - E:\code_github\LiveTalking3\LiveTalking\.planning\phases\04-llm-integration\04-CONTEXT.md (D-01, D-02, D-03, D-05 决策)
  </read_first>
  <action>
    修改 llm_response 函数，在 OpenAI API 调用之前添加 RAG 检索逻辑。具体修改：

    1. 在函数开头（`opt = avatar_session.opt` 之后）添加对话历史初始化：
       ```python
       # Initialize conversation history if not exists
       if not hasattr(avatar_session, '_llm_history'):
           avatar_session._llm_history = []
       ```

    2. 在 messages 构建之前（约第 21-24 行）添加 RAG 检索逻辑：
       ```python
       # RAG retrieval for chat mode (enhanced prompt)
       enhanced_message = message
       if rag_retriever and getattr(opt, 'rag_enabled', False):
           try:
               # Build retrieval query with conversation context
               retrieval_query = message
               if avatar_session._llm_history:
                   # Include last 2 turns for context
                   context_msgs = avatar_session._llm_history[-4:]
                   retrieval_query = " ".join([m["content"] for m in context_msgs]) + " " + message
               
               # Retrieve relevant documents
               retrieved = rag_retriever.retrieve(retrieval_query)
               
               if retrieved:
                   enhanced_message = build_rag_prompt(message, retrieved)
                   logger.info(f"RAG retrieved {len(retrieved)} documents for query")
           except Exception as e:
               logger.warning(f"RAG retrieval failed, using original message: {e}")
               # Fallback: use original message (silent degradation per D-03)
       ```

    3. 修改 messages 列表，使用 enhanced_message：
       ```python
       messages=[{'role': 'system', 'content': '你是一个知识助手，尽量以简短、口语化的方式输出'},
                 {'role': 'user', 'content': enhanced_message}],
       ```

    4. 在流式输出结束后（`avatar_session.put_msg_txt(result,datainfo)` 之后），更新对话历史：
       ```python
       # Update conversation history
       avatar_session._llm_history.append({'role': 'user', 'content': message})
       # Note: assistant response not captured here due to streaming; could be added if needed
       ```

    注意：
    - 保持现有流式输出逻辑不变
    - 使用 try-except 实现静默降级 (D-03)
    - 对话历史存储在 avatar_session._llm_history 中 (D-05)
    - 默认不启用 RAG，保持 echo 模式兼容 (FR-4.4)
  </action>
  <verify>
    <automated>python -c "
import llm
import inspect
source = inspect.getsource(llm.llm_response)
assert 'rag_retriever' in source
assert 'build_rag_prompt' in source
assert 'enhanced_message' in source
print('PASS')
"</automated>
  </verify>
  <acceptance_criteria>
    - llm_response 函数包含 `if rag_retriever and getattr(opt, 'rag_enabled', False):` 条件判断
    - llm_response 函数调用 `rag_retriever.retrieve()` 执行检索
    - llm_response 函数调用 `build_rag_prompt()` 构建增强 prompt
    - llm_response 函数包含 try-except 异常处理（静默降级）
    - llm_response 函数维护 `avatar_session._llm_history` 对话历史
    - 流式输出逻辑未被修改（for chunk in completion 循环保持不变）
  </acceptance_criteria>
  <done>llm_response 函数集成 RAG 检索，支持知识库上下文注入、多轮对话历史、静默降级。</done>
</task>

<task type="auto">
  <name>Task 3: 修改 app.py 设置 llm 模块的 rag_retriever</name>
  <files>app.py</files>
  <read_first>
    - E:\code_github\LiveTalking3\LiveTalking\app.py (当前 RAG 初始化逻辑)
    - E:\code_github\LiveTalking3\LiveTalking\llm.py (rag_retriever 模块变量)
  </read_first>
  <action>
    在 app.py 的 RAG 初始化成功后，设置 llm 模块的 rag_retriever 变量：

    在 main() 函数的 RAG 初始化逻辑中，在 `rag_retriever = RAGRetriever(...)` 之后添加：
    ```python
    # Share retriever with llm module
    import llm
    llm.rag_retriever = rag_retriever
    ```

    完整的 RAG 初始化代码块应该是：
    ```python
    # Initialize RAG retriever if enabled
    if opt.rag_enabled:
        try:
            _rag_embedding = DashScopeEmbedding()
            _rag_store = VectorStore(persist_dir=opt.rag_persist_dir, collection_name=opt.rag_collection)
            rag_retriever = RAGRetriever(_rag_store, _rag_embedding, top_k=opt.rag_top_k)
            logger.info(f"RAG retriever initialized: {opt.rag_persist_dir}, top_k={opt.rag_top_k}, docs={rag_retriever.count()}")
            # Share retriever with llm module
            import llm
            llm.rag_retriever = rag_retriever
        except Exception as e:
            logger.warning(f"Failed to initialize RAG retriever, RAG disabled: {e}")
            rag_retriever = None
    ```
  </action>
  <verify>
    <automated>python -c "
import inspect
with open('app.py', 'r') as f:
    content = f.read()
assert 'llm.rag_retriever = rag_retriever' in content
print('PASS')
"</automated>
  </verify>
  <acceptance_criteria>
    - app.py 在 RAG 初始化成功后执行 `llm.rag_retriever = rag_retriever`
    - llm 模块的 rag_retriever 变量被正确设置
  </acceptance_criteria>
  <done>app.py 正确设置 llm 模块的 rag_retriever 变量，实现跨模块共享。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → RAG query | 用户输入构建检索查询 |
| RAG result → LLM prompt | 检索结果注入 prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Information Disclosure | RAG chunks | accept | 检索结果来自可信知识库 |
| T-04-03-02 | Denial of Service | retrieval | accept | 检索失败静默降级 |
| T-04-03-03 | Tampering | history | accept | 历史记录在 avatar_session 中隔离 |
</threat_model>

<verification>
- 运行 `python -c "import llm; print(llm.rag_retriever)"` 确认变量存在
- 检查 llm.llm_response 函数源代码包含 rag_retriever 检索逻辑
- 检查 app.py 正确设置 llm.rag_retriever
</verification>

<success_criteria>
- llm.py 包含 RAG 检索逻辑
- rag_enabled=True 时执行检索，rag_enabled=False 时跳过
- 检索失败时静默降级，使用原始 message
- 对话历史正确维护
- 流式输出机制未被破坏
</success_criteria>

<output>
After completion, create `.planning/phases/04-llm-integration/04-03-SUMMARY.md`
</output>
