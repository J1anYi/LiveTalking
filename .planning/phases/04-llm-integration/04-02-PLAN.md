---
phase: 04-llm-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01-PLAN.md
files_modified:
  - app.py
autonomous: true
requirements:
  - FR-4.1

must_haves:
  truths:
    - "应用启动时初始化全局 RAG 检索器实例"
    - "RAG 检索器在 opt.rag_enabled=True 时才初始化"
    - "全局 RAG 检索器实例被 llm.py 使用"
  artifacts:
    - path: "app.py"
      provides: "全局 RAG 检索器实例 rag_retriever"
      contains: "rag_retriever"
  key_links:
    - from: "app.py"
      to: "llm.py"
      via: "模块级全局变量"
      pattern: "from rag import"
---

<objective>
在 app.py 中初始化全局 RAG 检索器实例，应用启动时根据配置创建并复用。

Purpose: 实现 D-06 决策 — 应用启动时初始化 RAG 检索器，全局复用实例，避免每次调用创建新实例。
Output: 修改后的 app.py，在启动流程中根据 rag_enabled 配置初始化全局 RAG 检索器。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-llm-integration/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 app.py 中添加全局 RAG 检索器初始化逻辑</name>
  <files>app.py</files>
  <read_first>
    - E:\code_github\LiveTalking3\LiveTalking\app.py (当前应用入口)
    - E:\code_github\LiveTalking3\LiveTalking\rag\__init__.py (RAG 公共 API: RAGRetriever, DashScopeEmbedding, VectorStore)
    - E:\code_github\LiveTalking3\LiveTalking\rag\retriever.py (RAGRetriever 类签名)
    - E:\code_github\LiveTalking3\LiveTalking\config.py (上一计划添加的 rag_enabled 等参数)
  </read_first>
  <action>
    在 app.py 中添加全局 RAG 检索器初始化逻辑。具体修改：

    1. 在文件顶部（约第 39 行 `from llm import llm_response` 之后）添加：
       ```python
       from rag import RAGRetriever, DashScopeEmbedding, VectorStore
       ```

    2. 在全局变量区域（约第 60 行 `global_avatars = {}` 之后）添加：
       ```python
       rag_retriever = None  # Global RAG retriever instance
       ```

    3. 在 main() 函数中，在 `opt = parse_args()` 之后（约第 112 行），在 avatar 模块加载之前，添加 RAG 初始化逻辑：
       ```python
       # Initialize RAG retriever if enabled
       if opt.rag_enabled:
           try:
               _rag_embedding = DashScopeEmbedding()
               _rag_store = VectorStore(persist_dir=opt.rag_persist_dir, collection_name=opt.rag_collection)
               rag_retriever = RAGRetriever(_rag_store, _rag_embedding, top_k=opt.rag_top_k)
               logger.info(f"RAG retriever initialized: {opt.rag_persist_dir}, top_k={opt.rag_top_k}, docs={rag_retriever.count()}")
           except Exception as e:
               logger.warning(f"Failed to initialize RAG retriever, RAG disabled: {e}")
               rag_retriever = None
       ```

    4. 在 main() 函数的 `global` 声明中（第 108 行 `global rtc_manager, opt, model,load_avatar`）添加 `rag_retriever`。

    注意事项：
    - 使用 DashScopeEmbedding() 不传参数时，它自动从 DASHSCOPE_API_KEY 环境变量获取 API key
    - VectorStore 的 collection_name 参数需确认是否支持，需检查 vector_store.py 构造函数签名
  </action>
  <verify>
    <automated>python -c "
from config import parse_args
import sys
sys.argv = ['test']
opt = parse_args()
# 验证 rag_enabled 默认为 False
assert opt.rag_enabled == False
# 验证参数存在
assert hasattr(opt, 'rag_top_k')
assert hasattr(opt, 'rag_persist_dir')
print('PASS')
"</automated>
  </verify>
  <acceptance_criteria>
    - app.py 包含 `from rag import RAGRetriever, DashScopeEmbedding, VectorStore` 导入
    - app.py 包含模块级变量 `rag_retriever = None`
    - main() 函数中 global 声明包含 `rag_retriever`
    - main() 函数中包含 `if opt.rag_enabled:` 条件初始化逻辑
    - 初始化失败时 rag_retriever 保持 None 并记录警告日志
    - 初始化成功时记录日志包含 persist_dir 和 top_k 信息
  </acceptance_criteria>
  <done>app.py 在启动时根据配置初始化全局 RAG 检索器实例，初始化失败时静默降级。</done>
</task>

<task type="auto">
  <name>Task 2: 验证 VectorStore 构造函数签名</name>
  <files>rag/vector_store.py</files>
  <read_first>
    - E:\code_github\LiveTalking3\LiveTalking\rag\vector_store.py (VectorStore 构造函数)
  </read_first>
  <action>
    读取 rag/vector_store.py 确认 VectorStore 构造函数的参数签名。特别检查：
    1. persist_dir 参数是否支持
    2. collection_name 参数是否支持
    3. 默认值是什么

    如果 VectorStore 不支持 collection_name 参数，则 Task 1 中的初始化代码需要相应调整（只传 persist_dir）。

    此任务是验证任务，不需要修改代码。只需确认 VectorStore 的构造函数签名，然后确保 Task 1 的代码与之匹配。
  </action>
  <verify>
    <automated>python -c "import inspect; from rag.vector_store import VectorStore; sig = inspect.signature(VectorStore.__init__); print(sig)"</automated>
  </verify>
  <acceptance_criteria>
    - 确认 VectorStore 构造函数支持的参数列表
    - Task 1 中的初始化代码参数与 VectorStore 实际签名一致
  </acceptance_criteria>
  <done>VectorStore 构造函数签名已确认，app.py 中的初始化代码与之匹配。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config → RAG init | 配置参数控制初始化行为 |
| RAG init → global state | 全局检索器实例影响所有请求 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Denial of Service | RAG init | accept | 初始化失败不阻止应用启动 |
| T-04-02-02 | Information Disclosure | persist_dir | accept | 路径由管理员配置 |
</threat_model>

<verification>
- 运行 `python -c "from app import rag_retriever; print(rag_retriever)"` 不启用 RAG 时应输出 None
- 检查 app.py 中 rag_retriever 初始化逻辑的完整性
</verification>

<success_criteria>
- app.py 在 rag_enabled=True 时初始化全局 RAG 检索器
- 初始化失败时静默降级，应用正常启动
- 全局 rag_retriever 变量可被 llm.py 引用
</success_criteria>

<output>
After completion, create `.planning/phases/04-llm-integration/04-02-SUMMARY.md`
</output>
