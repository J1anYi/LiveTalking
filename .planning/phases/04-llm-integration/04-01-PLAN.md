---
phase: 04-llm-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
autonomous: true
requirements:
  - FR-5.1
  - FR-5.2
  - FR-5.3

must_haves:
  truths:
    - "用户可以通过 --rag_enabled 启用知识库检索"
    - "用户可以通过 --rag_top_k 配置检索数量"
    - "用户可以通过 --rag_persist_dir 配置知识库路径"
  artifacts:
    - path: "config.py"
      provides: "RAG CLI 参数定义"
      contains: "rag_enabled"
  key_links:
    - from: "config.py"
      to: "avatar_session.opt"
      via: "argparse"
      pattern: "add_argument.*--rag_"
---

<objective>
扩展 config.py 添加 RAG 知识库相关 CLI 参数。

Purpose: 为 RAG 功能提供配置入口，支持通过命令行启用/禁用知识库、配置检索参数。
Output: 扩展后的 config.py，包含 rag_enabled, rag_top_k, rag_persist_dir 等参数。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-llm-integration/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 RAG 配置参数到 config.py</name>
  <files>config.py</files>
  <read_first>
    - E:\code_github\LiveTalking3\LiveTalking\config.py (当前配置实现)
    - E:\code_github\LiveTalking3\LiveTalking\.planning\phases\04-llm-integration\04-CONTEXT.md (D-04: 通过 avatar_session.opt 传递配置)
  </read_first>
  <action>
    在 config.py 的 parse_args() 函数中添加 RAG 相关 CLI 参数。在 "# ─── 传输 ─────────────────────────────────────────────────────" 注释块之后添加新的 "# ─── RAG 知识库 ────────────────────────────────────────────────────" 注释块，包含以下参数：

    1. `--rag_enabled` - type=bool, default=False, help="enable RAG knowledge base retrieval"
    2. `--rag_top_k` - type=int, default=3, help="number of documents to retrieve"
    3. `--rag_persist_dir` - type=str, default="./data/chromadb", help="chromadb persistence directory"
    4. `--rag_collection` - type=str, default="knowledge_base", help="chromadb collection name"

    注意：argparse 没有 type=bool，需要使用 action='store_true' 实现 rag_enabled。
    其他参数使用 parser.add_argument 正常添加。
  </action>
  <verify>
    <automated>python -c "from config import parse_args; import sys; sys.argv = ['test', '--rag_enabled', '--rag_top_k', '5']; opt = parse_args(); assert opt.rag_enabled == True; assert opt.rag_top_k == 5; print('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - config.py 包含 "--rag_enabled" 参数定义
    - config.py 包含 "--rag_top_k" 参数定义
    - config.py 包含 "--rag_persist_dir" 参数定义
    - config.py 包含 "--rag_collection" 参数定义
    - 默认值：rag_enabled=False, rag_top_k=3, rag_persist_dir="./data/chromadb", rag_collection="knowledge_base"
  </acceptance_criteria>
  <done>config.py 包含完整 RAG 配置参数，可通过 CLI 设置知识库启用状态、检索数量和存储路径。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → config | 命令行参数由用户控制，需要验证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | config.py | accept | 参数类型由 argparse 强制，路径由后续代码验证 |
</threat_model>

<verification>
- 运行 `python -c "from config import parse_args; opt = parse_args(); print(opt.rag_enabled)"` 确认参数存在
- 运行 `python app.py --help` 确认 RAG 参数出现在帮助信息中
</verification>

<success_criteria>
- config.py 包含 rag_enabled, rag_top_k, rag_persist_dir, rag_collection 四个 RAG 参数
- 参数可通过命令行正确解析
- 默认值符合设计要求
</success_criteria>

<output>
After completion, create `.planning/phases/04-llm-integration/04-01-SUMMARY.md`
</output>
