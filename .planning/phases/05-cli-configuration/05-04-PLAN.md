---
phase: 05-cli-configuration
plan: 04
type: execute
wave: 2
depends_on: [05-01, 05-02, 05-03]
files_modified: [config.py, rag/config_loader.py, rag/__init__.py]
autonomous: true
requirements: [FR-5.1, FR-5.2, FR-5.3]
must_haves:
  truths:
    - "配置优先级为 CLI > 环境变量 > 配置文件"
    - "CLI --rag_config 参数可指定配置文件路径"
    - "parse_args() 返回合并后的配置"
    - "配置来源被记录到日志"
  artifacts:
    - path: "config.py"
      provides: "配置合并逻辑"
      contains: ["--rag_config", "merge_rag_config"]
    - path: "rag/config_loader.py"
      provides: "配置合并函数"
      exports: ["merge_rag_config"]
  key_links:
    - from: "config.py"
      to: "rag/config_loader.py"
      via: "import"
      pattern: "from rag import load_rag_config, load_rag_config_from_env, merge_rag_config"
---

# Phase 5 Plan 04: Configuration Merge with Priority

<objective>
实现配置优先级合并逻辑：CLI > 环境变量 > 配置文件，并修改 `config.py` 使用合并后的配置。

Purpose: 统一配置管理，实现优先级覆盖机制
Output: 更新后的 config.py 和 rag/config_loader.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@config.py
@rag/__init__.py
@rag/config_loader.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Configuration Merge Function</name>
  <files>rag/config_loader.py</files>
  <read_first>rag/config_loader.py</read_first>
  <action>在 `rag/config_loader.py` 中添加函数 `merge_rag_config()`：

```python
def merge_rag_config(cli_config: dict, env_config: dict, file_config: dict) -> dict:
    """Merge RAG configuration with priority: CLI > env > file.
    
    Args:
        cli_config: Configuration from CLI arguments
        env_config: Configuration from environment variables
        file_config: Configuration from YAML file
    
    Returns:
        Merged configuration dict
    """
    # Start with defaults
    result = get_default_rag_config()
    
    # Apply file config (lowest priority)
    for key, value in file_config.items():
        if value is not None:
            result[key] = value
    
    # Apply env config (medium priority)
    for key, value in env_config.items():
        if value is not None:
            result[key] = value
    
    # Apply CLI config (highest priority)
    for key, value in cli_config.items():
        if value is not None:
            result[key] = value
    
    return result
```

添加到 `rag/__init__.py` 导出。</action>
  <verify>
    <automated>
python -c "
from rag import merge_rag_config
# Test priority
result = merge_rag_config({'top_k': 5}, {'top_k': 3}, {'top_k': 2})
assert result['top_k'] == 5, 'CLI priority failed'
result = merge_rag_config({}, {'top_k': 3}, {'top_k': 2})
assert result['top_k'] == 3, 'Env priority failed'
result = merge_rag_config({}, {}, {'top_k': 2})
assert result['top_k'] == 2, 'File priority failed'
print('✓ merge_rag_config priority logic correct')
"
    </automated>
  </verify>
  <done>
    - `merge_rag_config()` 函数实现
    - 优先级正确：CLI > 环境变量 > 配置文件
    - 函数在 `rag/__init__.py` 中导出
  </done>
  <acceptance_criteria>
    - grep -q "def merge_rag_config" rag/config_loader.py
    - grep -q "merge_rag_config" rag/__init__.py
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add --rag_config CLI Parameter</name>
  <files>config.py</files>
  <read_first>config.py</read_first>
  <action>在 `config.py` 的 `parse_args()` 函数中添加新参数：

在 RAG 配置区块（`--rag_collection` 之后）添加：
```python
parser.add_argument('--rag_config', type=str, default='data/rag_config.yaml',
                    help="RAG configuration file path (YAML)")
```</action>
  <verify>
    <automated>
python -c "from config import parse_args; args = parse_args([]); print(f'✓ --rag_config default: {args.rag_config}')"
    </automated>
  </verify>
  <done>
    - `--rag_config` 参数添加
    - 默认值为 `data/rag_config.yaml`
  </done>
  <acceptance_criteria>
    - grep -q "\-\-rag_config" config.py
    - grep -q "data/rag_config.yaml" config.py
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Integrate Configuration Loading in parse_args()</name>
  <files>config.py</files>
  <read_first>config.py, rag/config_loader.py</read_first>
  <action>修改 `config.py` 的 `parse_args()` 函数：

1. 在文件顶部导入：
```python
from rag import load_rag_config, load_rag_config_from_env, merge_rag_config
```

2. 在 `opt = parser.parse_args()` 之后，`return opt` 之前添加：
```python
    # ─── RAG 配置合并 ────────────────────────────────────────────────
    # 加载配置文件
    file_config = load_rag_config(opt.rag_config)
    
    # 加载环境变量
    env_config = load_rag_config_from_env()
    
    # CLI 配置（只包含用户显式设置的值）
    cli_config = {}
    # 注意：argparse 的 store_true action 无法判断是否显式设置
    # 对于 boolean，我们依赖环境变量/文件配置作为默认值
    if opt.rag_top_k != 3:  # 非默认值表示用户设置
        cli_config["top_k"] = opt.rag_top_k
    if opt.rag_persist_dir != "./data/chromadb":
        cli_config["persist_dir"] = opt.rag_persist_dir
    if opt.rag_collection != "knowledge_base":
        cli_config["collection"] = opt.rag_collection
    
    # 合并配置
    merged = merge_rag_config(cli_config, env_config, file_config)
    
    # 应用合并后的配置
    opt.rag_enabled = merged.get("enabled", False)
    opt.rag_top_k = merged.get("top_k", 3)
    opt.rag_persist_dir = merged.get("persist_dir", "./data/chromadb")
    opt.rag_collection = merged.get("collection", "knowledge_base")
    
    # 记录配置来源
    from utils.logger import logger
    logger.info(f"RAG config: enabled={opt.rag_enabled}, top_k={opt.rag_top_k}, "
                f"persist_dir={opt.rag_persist_dir}, collection={opt.rag_collection}")
```

注意：`--rag_enabled` 是 store_true action，需要特殊处理。
如果 CLI 设置了 `--rag_enabled`，应该覆盖其他配置。检测方法：
检查 `sys.argv` 中是否包含 `--rag_enabled`。</action>
  <verify>
    <automated>
python -c "
import os
os.environ['RAG_TOP_K'] = '7'
from config import parse_args
args = parse_args(['--rag_top_k', '10'])
print(f'CLI top_k=10, env top_k=7 → result: {args.rag_top_k}')
assert args.rag_top_k == 10, 'CLI should override env'
print('✓ Configuration merge works')
"
    </automated>
  </verify>
  <done>
    - `config.py` 导入配置加载函数
    - `parse_args()` 实现配置合并逻辑
    - 优先级正确
    - 配置来源记录到日志
  </done>
  <acceptance_criteria>
    - grep -q "from rag import" config.py
    - grep -q "load_rag_config" config.py
    - grep -q "load_rag_config_from_env" config.py
    - grep -q "merge_rag_config" config.py
    - grep -q "file_config = load_rag_config" config.py
    - grep -q "env_config = load_rag_config_from_env" config.py
    - grep -q "merged = merge_rag_config" config.py
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Update rag/__init__.py exports</name>
  <files>rag/__init__.py</files>
  <read_first>rag/__init__.py, rag/config_loader.py</read_first>
  <action>确保 `rag/__init__.py` 导出 `merge_rag_config`：

```python
from .config_loader import merge_rag_config
```

在 `__all__` 中添加 `"merge_rag_config"`。</action>
  <verify>
    <automated>python -c "from rag import merge_rag_config; print('✓ merge_rag_config exported')"</automated>
  </verify>
  <done>
    - `merge_rag_config` 在 `rag/__init__.py` 中导出
  </done>
  <acceptance_criteria>
    - grep -q "merge_rag_config" rag/__init__.py
  </acceptance_criteria>
</task>

</tasks>

<verification>
运行所有 verify 命令，确保：
- 配置合并逻辑正确
- 优先级正确
- CLI 参数工作正常
</verification>

<success_criteria>
- `merge_rag_config()` 函数实现并导出
- `--rag_config` CLI 参数添加
- `parse_args()` 集成配置加载逻辑
- 优先级正确：CLI > 环境变量 > 配置文件
- 合并后的配置写回 `opt` 对象
</success_criteria>

<output>
After completion, create `.planning/phases/05-cli-configuration/05-04-SUMMARY.md`
</output>