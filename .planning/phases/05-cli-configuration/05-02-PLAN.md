---
phase: 05-cli-configuration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [rag/config_loader.py, rag/__init__.py]
autonomous: true
requirements: [FR-5.4]
must_haves:
  truths:
    - "环境变量 RAG_ENABLED 可被读取并转换为 bool"
    - "环境变量 RAG_TOP_K 可被读取并转换为 int"
    - "环境变量 RAG_PERSIST_DIR 可被读取"
    - "环境变量 RAG_COLLECTION 可被读取"
    - "未设置的环境变量不在返回字典中"
  artifacts:
    - path: "rag/config_loader.py"
      provides: "环境变量配置加载函数"
      exports: ["load_rag_config_from_env"]
  key_links:
    - from: "config.py"
      to: "rag/config_loader.py"
      via: "import"
      pattern: "from rag import load_rag_config_from_env"
---

# Phase 5 Plan 02: Environment Variable Configuration

<objective>
实现环境变量配置支持，使用 `RAG_*` 前缀读取配置。

Purpose: 支持容器化部署场景的配置需求
Output: `rag/config_loader.py` 中添加环境变量加载函数
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@rag/__init__.py
@rag/config_loader.py
@config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Environment Variable Loader</name>
  <files>rag/config_loader.py</files>
  <read_first>rag/config_loader.py, config.py</read_first>
  <action>在 `rag/config_loader.py` 中添加函数 `load_rag_config_from_env() -> dict`：

```python
def load_rag_config_from_env() -> dict:
    """Load RAG configuration from environment variables.
    
    Environment variables:
        RAG_ENABLED: "true", "1", "yes" → True; "false", "0", "no" → False
        RAG_TOP_K: Integer value for top_k
        RAG_PERSIST_DIR: String path for persist directory
        RAG_COLLECTION: String collection name
    
    Returns:
        Dict with only the environment variables that are set.
    """
    config = {}
    
    # RAG_ENABLED - boolean conversion
    enabled = os.environ.get("RAG_ENABLED")
    if enabled is not None:
        config["enabled"] = enabled.lower() in ("true", "1", "yes")
    
    # RAG_TOP_K - integer conversion
    top_k = os.environ.get("RAG_TOP_K")
    if top_k is not None:
        try:
            config["top_k"] = int(top_k)
        except ValueError:
            logger.warning(f"Invalid RAG_TOP_K value: {top_k}, ignoring")
    
    # RAG_PERSIST_DIR - string
    persist_dir = os.environ.get("RAG_PERSIST_DIR")
    if persist_dir is not None:
        config["persist_dir"] = persist_dir.strip()
    
    # RAG_COLLECTION - string
    collection = os.environ.get("RAG_COLLECTION")
    if collection is not None:
        config["collection"] = collection.strip()
    
    return config
```

确保在文件顶部导入 `os`（如果没有）。</action>
  <verify>
    <automated>
python -c "
import os
os.environ['RAG_ENABLED'] = 'true'
os.environ['RAG_TOP_K'] = '5'
from rag import load_rag_config_from_env
config = load_rag_config_from_env()
assert config['enabled'] == True
assert config['top_k'] == 5
print('✓ Environment variable loading works')
"
    </automated>
  </verify>
  <done>
    - `load_rag_config_from_env()` 函数实现
    - 支持 4 个环境变量：RAG_ENABLED, RAG_TOP_K, RAG_PERSIST_DIR, RAG_COLLECTION
    - 正确的类型转换
  </done>
  <acceptance_criteria>
    - grep -q "def load_rag_config_from_env" rag/config_loader.py
    - grep -q "RAG_ENABLED" rag/config_loader.py
    - grep -q "RAG_TOP_K" rag/config_loader.py
    - grep -q "RAG_PERSIST_DIR" rag/config_loader.py
    - grep -q "RAG_COLLECTION" rag/config_loader.py
    - grep -q "load_rag_config_from_env" rag/__init__.py
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update rag/__init__.py exports</name>
  <files>rag/__init__.py</files>
  <read_first>rag/__init__.py, rag/config_loader.py</read_first>
  <action>修改 `rag/__init__.py`：

1. 在导入区域添加（如果还没有）：
```python
from .config_loader import load_rag_config_from_env
```

2. 在 `__all__` 列表中添加：
```python
"load_rag_config_from_env",
```</action>
  <verify>
    <automated>python -c "from rag import load_rag_config_from_env; print('✓ load_rag_config_from_env exported')"</automated>
  </verify>
  <done>
    - 函数在 `rag/__init__.py` 中导出
    - 可以成功 import
  </done>
  <acceptance_criteria>
    - grep -q "load_rag_config_from_env" rag/__init__.py
  </acceptance_criteria>
</task>

</tasks>

<verification>
运行所有 verify 命令，确保：
- 环境变量正确读取
- 类型转换正确
- 函数导出正确
</verification>

<success_criteria>
- `load_rag_config_from_env()` 函数实现
- 支持所有 4 个环境变量
- 正确的类型转换（bool, int, str）
- 无效值优雅忽略
- 函数在 `rag/__init__.py` 中导出
</success_criteria>

<output>
After completion, create `.planning/phases/05-cli-configuration/05-02-SUMMARY.md`
</output>